// NPM is the standarf package manager for nodeJs

// Node Package Manager

// 1. it is a library of packages.
// 2. it is a command line tool.

// installing packages npn install 'package name', npm i 'package name'

// var figlet = require("figlet");

// figlet("Jatin verma", function (err, data) {
//   if (err) {
//     console.log("Something went wrong...");
//     console.dir(err);
//     return;
//   }
//   console.log(data);
// });


// require v/s import we can use import but why?
// we can't selectively load only the pices we need with require import, we can selectively load only the pices we need, which can save memory.

//loading is synchronous for 'require' but can be asynchronous for 'import'